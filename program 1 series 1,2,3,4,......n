#include<stdio.h>
int main()
{
	int i,n;
	printf("print number seris till:");
	scanf("%d",&n);
	printf("\n");
	for(i=1;i<n;i++)
	{
		if(i==n)
		printf("%d",i);
		else
		printf("%d",i);
	}
	return 0;
}
